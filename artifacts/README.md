# Artifacts

This directory stores saved work products for feature delivery and exploratory knowledge capture.

- `features/` contains per-feature specs, plans, tasks, and decision logs.
- `knowledge/` contains investigation notes that are not yet durable enough for `memories/`.
